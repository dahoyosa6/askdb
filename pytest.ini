[pytest]
# asyncio_mode = auto: las funciones de test `async def` se ejecutan como
# corrutinas sin necesidad de marcarlas una por una con @pytest.mark.asyncio.
# Lo usan las pruebas de los handlers async de Telegram (tests/test_telegram_handlers.py).
asyncio_mode = auto
